package com.vpf.repository;

import com.vpf.entity.FarmSettings;
import org.springframework.data.jpa.repository.JpaRepository;

public interface FarmSettingsRepository extends JpaRepository<FarmSettings, Long> {
}
